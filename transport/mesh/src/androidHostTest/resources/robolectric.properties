# Library modules expose compileSdk (37) as the test targetSdk, which exceeds the
# max SDK bundled with Robolectric 4.16.1 (36). Pin the test runtime to a supported
# SDK, matching the app module's targetSdk.
sdk=35
