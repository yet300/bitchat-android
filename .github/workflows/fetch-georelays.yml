name: Fetch GeoRelays Data

on:
  schedule:
    - cron: '0 6 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-relay-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Fetch GeoRelays
        run: |
          wget -q https://raw.githubusercontent.com/permissionlesstech/georelays/refs/heads/main/nostr_relays.csv -O nostr_relays.csv
          # Ship the same list to both platform bundles (Android assets + iOS app bundle).
          cp nostr_relays.csv androidApp/src/main/assets/nostr_relays.csv
          cp nostr_relays.csv iosApp/iosApp/nostr_relays.csv
          rm -f nostr_relays.csv

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code || echo "changes=true" >> $GITHUB_OUTPUT
    
      - name: Commit and push changes
        if: steps.git-check.outputs.changes == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add androidApp/src/main/assets/nostr_relays.csv iosApp/iosApp/nostr_relays.csv
          git commit -m "Automated update of relay data - $(date -u)"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}